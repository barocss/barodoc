---
interface Props {
  cols?: 1 | 2 | 3;
}

const { cols = 2 } = Astro.props;

const gridCols = {
  1: "grid-cols-1",
  2: "grid-cols-1 md:grid-cols-2",
  3: "grid-cols-1 md:grid-cols-2 lg:grid-cols-3",
};
---

<div class:list={["not-prose grid gap-4 my-4", gridCols[cols]]}>
  <slot />
</div>
